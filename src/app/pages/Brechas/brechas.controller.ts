import { Request, Response } from "express";
import prisma from "../../../utils/prisma";

export const getDataBrechas = async (req: Request, res: Response) => {
	const { distritoId, periodoValue, impacto } = req.params;
	try {
	} catch (error) {}
};
